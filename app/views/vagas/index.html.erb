  <div class="wrapper">
    <div class="section features-6">
      <div class="container">
    
                
<table class="table table-sm">
<thead>
  <tr>
    <th scope="col">#</th>
    <th>Quadra</th>
    <th>Tipo</th>
    <th scope="col" colspan="3">Ações</th>
  </tr>
</thead>
<tbody>
<% @vagas.each do |vaga| %>
  <tr>
  <th scope="row"><%= vaga.id %></th>
  <td><%= vaga.quadra %></td>
  <td><%= vaga.tipo %></td>
  <td><%= link_to 'Consultar', vaga %></td>
  <td><%= link_to 'Editar', edit_vaga_path(vaga) %></td>
  <td><%= link_to 'Excluir', vaga, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
  </tr>
  <% end %>
</tbody>
</table>

<a href="/vagas/new" class="btn btn-primary">Novo</a>

      </div>
    </div>
    <div class="section features-1">
<div class="container">    
</div>
    </div>
    <br /><br />