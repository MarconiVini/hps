<%= form_with(model: vaga, local: true) do |form| %>
  <% if vaga.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vaga.errors.count, "error") %> prohibited this vaga from being saved:</h2>

      <ul>
      <% vaga.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card">
  <h5 class="card-header">Dados da vaga</h5>
    <div class="card-body">
      <form class="needs-validation" novalidate>
        <div class="form-row">
          <div class="form-group form-group col-md-6 mb-3">
            <%= form.label :quadra %>
            <%= form.text_field :quadra, id: :vaga_quadra, class: 'form-control form-control-sm', required: true %>
          </div>

          <div class="form-group form-group col-md-6 mb-3">
            <%= form.label :tipo %>
            <%= form.text_field :tipo, id: :vaga_tipo, class: 'form-control form-control-sm', required: true, type: 'text' %>
          </div>
        </div>

          <div class="actions form-group">
            <%= form.submit %>
          </div>
          </div>
          </div>
<% end %>
